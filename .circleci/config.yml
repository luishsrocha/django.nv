jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "This is a build step"

  test:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "This is a test step"

  # ADICIONE ESTE JOB QUE ESTÁ FALTANDO
  integration:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "Running integration tests"
      # Adicione seus comandos de teste de integração aqui

  zap_baseline:
    machine: true
    steps:
      - checkout
      - run: |
          docker pull hysnsec/zap:2.16.1
          docker run --user root --rm -v $(pwd):/zap/wrk:rw -w /zap hysnsec/zap:2.16.1 zap-baseline.py -t https://prod-4srhyn66.lab.practical-devsecops.training -J zap-output.json || true
      - store_artifacts:
          path: zap-output.json
          destination: zap-artifact

  prod:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: echo "This is a deploy step"

workflows:
  version: 2
  django:
    jobs:
      - build
      - test:
          requires:
            - build
      - integration:    # AGORA ESTE JOB EXISTE!
          requires:
            - test
      - zap_baseline:
          requires:
            - integration
      - prod:
          type: approval
          requires:
            - zap_baseline
